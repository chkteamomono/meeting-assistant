# 실제 회의 내용(녹음·녹취록·회의록·안건판) — 절대 올리면 안 됨
03. 회의 기록/

# 민감정보(노션 API 키 등) — 절대 올리면 안 됨
설정.json
.env

# 실행 중에만 생기는 파일 — 코드가 아님
02. 회의 보조 비서 실행하기/실행로그.txt
02. 회의 보조 비서 실행하기/실행중.pid

# 파이썬 캐시
__pycache__/
*.pyc
참고자료/받아쓰기_용어.txt

# 면접 — 지원자 PII(이력서·이름·면접 대화록), 절대 올리면 안 됨
면접/03. 면접자 정보/

# 면접 — 실제 회사명·채용기준·질문뱅크 포함(구조가 아니라 실제 운영 내용)
면접/01. 페르소나설정/
면접/참고자료/

# 면접 — 실행 중에만 생기는 파일
면접/02. 면접 보조 비서 실행하기/실행로그.txt
면접/02. 면접 보조 비서 실행하기/실행중.pid

# 런처 — 실행 중에만 생기는 파일
00. 시작하기/실행중.pid
